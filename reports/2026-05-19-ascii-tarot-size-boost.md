# ASCII Tarot: увеличение размера ASCII-блока

Дата: 2026-05-19

Что изменил:

- увеличил размер ASCII-отрисовки для колоды `ASCII Tarot`;
- поднял `font-size` и межстрочный шаг в генераторе, чтобы рисунок занимал больше площади внутри карты;
- уменьшил минимальный левый отступ, чтобы блок не терял ширину на широких строках;
- перегенерировал все `78/78` SVG-ассетов колоды.

Проверки:

- `node scripts/generate-ascii-tarot-assets.mjs`
- `git diff --check`

Итог:

- ASCII-блок стал заметно крупнее и ближе к размерам подложки карты;
- генерация остаётся детерминированной;
- сторонние ассеты и UI-интеграцию не трогал.
