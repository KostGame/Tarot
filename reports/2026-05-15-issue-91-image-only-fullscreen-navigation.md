# Issue #91 — image-only fullscreen navigation

## Что изменено
- Добавил перелистывание в image-only fullscreen для текущего списка карт.
- Добавил свайпы влево/вправо для перехода между картами.
- Добавил свайп вниз для закрытия image-only fullscreen.
- Добавил полупрозрачные стрелки влево/вправо с удобной tap-зоной.
- Добавил нижнюю плашку с названием карты, native title при отличии и подписью колоды.
- Заменил тяжелый крестик на легкий светлый крестик без черного круга.
- Сохранил обычный fullscreen с описанием, guide fullscreen, share canvas и Firebase counter без изменений.

## Файлы
- `index.html`

## Проверки
- `git diff --check`
- inline JS syntax check через `node` + `new Function(...)`
- browser smoke-test через Chromium + CDP на мобильной ширине `390px`

## Smoke-test
- Открыл `Справочник карт`.
- Переключился на фильтр `Старшие`.
- Открыл карточку из справочника.
- Открыл image-only fullscreen.
- Проверил, что навигация показывает стрелки и нижнюю плашку.
- Проверил переход кнопками `влево/вправо`.
- Проверил свайп влево для перехода к следующей карте.
- Проверил свайп вниз для закрытия image-only fullscreen.
- Визуально посмотрел снимок image-only экрана.

## Результат
- Карточки листаются.
- Кнопки навигации работают.
- Свайпы работают.
- Нижняя плашка читается и не перекрывает карту.
- Крестик стал визуально легче.
- Горизонтальный скролл на проверке не проявился.

## Примечания
- `package.json` в этом worktree отсутствует, поэтому `npm run check`, `npm test` и `npm run build` недоступны.
