# ASCII Tarot layout fix report

- Дата: `2026-05-19`
- Ветка: `fix/ascii-tarot-layout-centering`
- Цель: увеличить сам ASCII-рендер в SVG и выровнять его по центру, чтобы он лучше заполнял карточку в списке и fullscreen.

## Что изменено

- В `scripts/generate-ascii-tarot-assets.mjs` увеличен размер шрифта ASCII-арта.
- Там же увеличен шаг строк, чтобы блок оставался читаемым и не упирался в края.
- Горизонтальное позиционирование стало вычисляться по ширине ASCII-блока, а не фиксированным смещением.
- Пересобраны все `78` SVG-карт колоды `ASCII Tarot`.

## Ожидаемый эффект

- Карты должны выглядеть крупнее в списке.
- Карты должны выглядеть крупнее в fullscreen.
- Смещение влево должно уйти.

## Проверки

- `node scripts/generate-ascii-tarot-assets.mjs`
- `git diff --check`

## Статус

- PR ещё не создан для этого follow-up фикса на момент написания отчёта.
