# Отчет по Issue #69

- Ветка: `feature/issue-69-guide-all-filter-reset`
- Commit: `9952a1a`
- Статус: PR будет открыт отдельно

## Что изменено

- При клике по фильтру `Все` в справочнике теперь очищается `guide-search-input`.
- Внутреннее значение поиска сбрасывается вместе с полем.
- Добавлен `guideSearchInput.blur()`, чтобы на телефоне закрывалась клавиатура.
- После сброса сразу перерендеривается полный список текущей колоды / текущего safe card pool.

## Что не менялось

- Остальные фильтры справочника.
- Избранное.
- Fullscreen navigation по справочнику.
- Firebase daily counter.
- Share canvas.
- Mapping колод.
- `assets/data/card-guide-content.json`.

## Проверки

- `git diff --check`
- Мобильный smoke-test через headless Chrome на ширине около 390px
- `package.json` в репозитории отсутствует, поэтому `npm run check`, `npm test` и `npm run build` недоступны

## Результат smoke-test

- После ввода `маг` список в справочнике фильтруется.
- Нажатие `Все` очищает поле поиска.
- После `Все` поле поиска теряет фокус.
- После `Все` список снова показывает полный набор карточек.
- Активен фильтр `Все`.
- Другие фильтры продолжают работать и не очищают поиск неожиданно.
- Справочник остаётся открытым, fullscreen поведение не затронуто.

